source("renv/activate.R")
if (file.exists(".env")) {
  dotenv::load_dot_env()
} else {
  message("No .env file found. Copy .env.example to .env and fill in your ",
          "local paths — see CLAUDE.md Environment Variables table.")
}
