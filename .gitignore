# ── Credentials — never commit ────────────────────────────────────────────────
.env

# ── Data directories — re-generated by pipeline ───────────────────────────────
# Adjust these for your project. As a general rule:
# - Committed: manifests, snapshots, overrides (small, human-readable, scientific decisions)
# - Gitignored: raw downloads, processed data, large binary files
data/raw/
data/extracted/
data/processed/
!data/processed/B2_SoilInfiltration_Results.csv

# ── Pipeline outputs — re-generated by pipeline ───────────────────────────────
outputs/
figures/
!figures/B2_SoilInfiltration_curves_*.png
*.meta.json

# ── R ─────────────────────────────────────────────────────────────────────────
.Rhistory
.Rapp.history
.RData
.Ruserdata
.Rproj.user/
renv/library/
renv/staging/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── Windows ───────────────────────────────────────────────────────────────────
Thumbs.db
.positai

# ── ASD hyperspectral data files (uploaded for testing, not source-controlled) ─
.devcontainer/ProcessedReflectance_*.txt
.devcontainer/SpectralID_*.csv
